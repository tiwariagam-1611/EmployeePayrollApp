UC6: Input Validation  
Goal: Validate all user inputs robustly.
Description: Centralizes validation logic in ValidationService.
Uses custom exception hierarchy, RegEx pattern matching, and sanitization to enforce fail-fast validation with user-friendly error messages.
